const multiplieur = myNumber => myNumber * 2

export default multiplieur

export const diviseur = myNumber => myNumber / 2
export const additionneur = myNumber => myNumber + 2
export const soustraction = myNumber => myNumber - 2