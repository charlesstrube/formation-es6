test('des11', () => {
    const alphabetGrec = {
        alpha: 'α',
        beta: 'β',
        gamma: 'γ',
        delta: 'δ',
        epsilode: 'ε'
    }

    const alpha = alphabetGrec.alpha
    const gamma = alphabetGrec.gamma

    console.log(alpha, gamma)
});