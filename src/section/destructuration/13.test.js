test('des13', () => {
    const alphabetGrec = {
        alpha: 'α',
        beta: 'β',
        gamma: 'γ',
        delta: 'δ',
        epsilode: 'ε'
    }

    const { alpha, delta } = alphabetGrec

    console.log(alpha, delta)
});